# Матрица покрытия: полный набор анти-скам функций

Дата: 2026-08-03. Назначение: зафиксировать, что базовая защита от «обычного»
интернет-скама у нас **полная**, а AI-слой стоит сверху, а не вместо неё.
Каждая строка — реальный вектор, наш ответ, метод детекта и веха.

Цифры и источники по каждому вектору — в [evidence/](evidence/):
[01](evidence/01-threat-landscape.md) (классический скам),
[02](evidence/02-ai-agent-threats.md) (AI-вектор),
[03](evidence/03-extensions.md) (расширения),
[04](evidence/04-credentials.md) (утечки).

Обозначения: **Л** — считается локально, наружу ничего не уходит;
**П** — уходит анонимный примитив (хэш-префикс);
**С** — сетевой запрос с идентификатором (только по явному действию пользователя).

> **Что это за документ, и чем он не является.** Матрица написана 2026-08-03,
> до реализации: это *целевой* набор покрытия, а не отчёт о том, что расширение
> умеет сегодня. Метки `v0.3`/`v0.6`/`v0.7` — планировочные и ничему в репозитории
> не соответствуют; релизы называются R1–R5.
>
> Что действительно реализовано и чем это доказано — в
> [приёмке](superpowers/audits/2026-08-05-acceptance.md) и в
> [сценариях](ux/scenarios.md), где у каждой строки стоит покрытие с `file:line`.
> Столбец **Готово** ниже отмечает `✓` те векторы, что закрыты кодом с тестами;
> строка без отметки — намерение, и читать её как возможность продукта нельзя.

---

## 1. Ссылки, домены, страницы

| # | Вектор | Что делаем | Метод детекта | Реж. | Веха | Готово |
|---|---|---|---|---|---|---|
| 1.1 | Фишинговая страница логина (имперсонация бренда) | блок + интерстишл с причиной | локальные фиды (OpenPhish/PhishTank/URLhaus/Phishing Army) + Safe Browsing Local List (хэш-префиксы) + признаки формы пароля на неизвестном домене | Л/П | v0.3 | **✓** REQ-14, e2e/scn-007 |
| 1.2 | **Typosquat, homoglyph, punycode/IDN** («аpple.com» → `xn--pple-43d.com`) | предупреждение «домен выглядит как X, но это не X» | декодирование punycode + Unicode confusables (skeleton) + расстояние до списка Tranco и до личного watchlist | **Л** | v0.3 | **✓** REQ-15, e2e/scn-006 |
| 1.3 | Открытые редиректы и цепочки перенаправлений | разворачиваем цепочку до конечного URL, проверяем его | `webNavigation` + разбор `?url=`/`?redirect=` параметров | Л | v0.3 | — |
| 1.4 | Malvertising: вредоносные объявления и редиректы по клику | блок конечного домена, пометка источника | DNR-правила + фиды + эвристика «клик → мгновенный кросс-доменный редирект» | Л | v0.3 | — |
| 1.5 | Скам-магазины (fake shop) | предупреждение перед вводом платёжных данных | возраст домена (NRD) + CT-запись + отсутствие контактов/юрлица + шаблонность + отсутствие в Tranco | Л/П | v0.7 | — |
| 1.6 | Tech-support scam («у вас вирус», полноэкранный лок, зацикленные диалоги) | разрыв ловушки: выход из fullscreen, отключение `beforeunload`-спама, баннер | эвристики страницы: `requestFullscreen` без действия, циклические `alert`, фейковый интерфейс ОС/браузера | **Л** | v0.3 | **✓** REQ-17, e2e/scn-008 |
| 1.7 | **ClickFix / фейковая CAPTCHA (paste-and-run)** — рост +517% (H1-2025 к H2-2024), 700+ взломанных сайтов в одной кампании (май 2026) | перехват: предупреждение в момент записи в буфер + баннер «настоящая CAPTCHA никогда не просит открыть Win+R» | детект записи в clipboard из скрипта + сигнатуры инструкций (Win+R, PowerShell, `mshta`, `curl\|iex`) + фейковый CAPTCHA-шаблон | **Л** | v0.3 | **✓** REQ-16, e2e/scn-008 |
| 1.8 | Browser-in-the-Browser (фейковое окно SSO внутри страницы) | пометка «это не настоящее окно браузера» | детект DOM-элемента, имитирующего рамку окна с адресной строкой + отсутствие реального `window.open` | **Л** | v0.6 | — |
| 1.9 | Quishing: QR-коды на странице | распознаём QR, показываем реальный URL и его вердикт до сканирования телефоном | локальный декодер QR из `<img>`/`<canvas>` + проверка URL | Л/П | v0.7 | — |
| 1.10 | Фейковые обновления браузера/плагина | блок загрузки + объяснение | шаблон страницы обновления + несоответствие домена вендору + проверка загрузки (см. §2) | Л | v0.3 | — |
| 1.11 | Домены-однодневки под скам-кампании | ранний флаг «домен зарегистрирован N часов назад» | CertStream (CT) + фид новых доменов, персональный watchlist по своим брендам | Л/П | v0.7 | **✓** REQ-11, e2e/scn-010 |
| 1.12 | Подмена поисковика и hijack выдачи | детект + возврат к выбранному поисковику | `chrome_settings_overrides` + `webNavigation` на цепочки поиска | **Л** | v0.7 | — |
| 1.13 | Спам push-уведомлений и навязанные подписки | блок запроса, чистка выданных разрешений | `contentSettings` + блок-лист доменов + аудит выданных прав | **Л** | v0.7 | — |
| 1.14 | Clipboard-hijack / подмена крипто-адреса (clipper) | предупреждение при подмене скопированного адреса | сравнение записанного в буфер значения с выделенным пользователем | **Л** | v0.6 | — |
| 1.15 | Ввод пароля/карты на подозрительном домене | предупреждение **до** отправки формы | форма + свежесть домена + отсутствие в личном списке доверенных | **Л** | v0.3 | **✓** REQ-18, SCN-011, e2e/scn-011; **форма во встроенном фрейме — SCN-034, e2e/scn-034** (до 2026-08-20 не проверялась вовсе: страж стоял под `if (isTopFrame)`, то есть самый частый случай — OAuth и платёжная форма в iframe — оставался вне покрытия, B-79). **Но без возраста домена**: NRD-запрос отправил бы адрес каждой страницы входа на сервер, и `core-credential/guard.ts` перечисляет это среди того, чего он не знает, вместо тихой подстановки «новый» |

## 2. Загрузки и файлы

| # | Вектор | Что делаем | Метод | Реж. | Веха | Готово |
|---|---|---|---|---|---|---|
| 2.1 | Загрузка вредоносного файла | предупреждение до сохранения | `chrome.downloads.onCreated`: репутация `item.url` по фидам. **Не `onDeterminingFilename` и не `finalUrl`** — строка обещала их до 2026-08-20, а код слушает `onCreated` и читает `item.url`, то есть адрес до редиректов; для ссылки, которая ведёт на файл через редирект, проверяется не тот адрес | Л | v0.3 | **✓** REQ-19, `apps/extension/src/background/downloads.ts`, `apps/extension/src/background/index.ts` |
| 2.2 | Известный вредоносный образец | блок по хэшу | **не делаем.** Проверка стоит между созданием элемента и записью байтов, то есть файла ещё нет — и хэш посчитать не по чему. Продукт это и говорит: `hash: { ran: false, why: 'downloadNotWritten' }`, то есть вердикт называет пропуск пропуском, а не чистотой. Пересчитать по повторно скачанным байтам значило бы скачать возможно вредоносный файл второй раз и стать вторым клиентом того же хоста; сверять по MalwareBazaar/VirusTotal — отправить хэш файла пользователя наружу, чего `docs/privacy.md` про `file-hash` прямо не обещает («никуда»), и держать ключ API в клиенте | П | — | — |
| 2.3 | Маскировка типа файла | флаг | двойное расширение (`.pdf.exe`), несоответствие MIME и расширения, исполняемые типы. **Предел:** MIME-проверка односторонняя — расширение против типа, не наоборот | **Л** | v0.3 | **✓** REQ-19, `packages/core-download/src/judge.ts` |
| 2.4 | Архив с паролем / обход сканеров | предупреждение о повышенном риске | тип + признак «пароль в тексте страницы» | **Л** | v0.6 | — |

**Честное ограничение:** расширение не читает файл с диска. Хэш считаем по
повторно скачанным байтам — работает для прямых ссылок, не работает за
авторизацией; там остаются URL-репутация и эвристики. Safe Browsing Download
Protection расширениям недоступен. Это пишем в UI.

## 3. Расширения браузера (Extension Guard) — полный набор проверок

Разовая проверка при установке бесполезна: расширения с 7-летней историей
доверия превращали в шпионское ПО тихими обновлениями (~4.3 млн устройств);
900k-пользовательское расширение содержало скрытую экспфильтрацию. **Ядро
риска — изменения во времени.**

| # | Сигнал | Как получаем | Реж. | Веха |
|---|---|---|---|---|---|
| 3.1 | Инвентарь и разрешения | `chrome.management` | **Л** | v0.5 | **✓** REQ-23, e2e/scn-017 |
| 3.2 | **Дельта разрешений между версиями** | снимок манифеста при каждом обновлении + диф | **Л** | v0.5 | **✓** REQ-23, e2e/scn-017 |
| 3.3 | **Смена владельца / издателя** | сравнение инвентаря между пробуждениями; `publisher` выводится из `updateUrl`, потому что стор не отдаёт поле автора. **Предел:** у расширений из Web Store `updateUrl` одинаков, поэтому настоящая смена владельца этим сигналом не видна — B-56 | С | v0.5 | **✓** REQ-24, `packages/core-extensions/src/diff.ts`, `apps/extension/src/background/extensions.ts` |
| 3.4 | Тихое обновление после долгого затишья | отслеживание частоты и дат релизов | Л/С | v0.5 | — |
| 3.5 | Обфускация кода | **пакет, который приносит человек** — плотность hex-экранирования и декодеры (`atob`, `String.fromCharCode`), плюс отметка «файл сжат». Энтропии и z-score нет; **разбора установленного CRX нет и быть не может** — браузер не отдаёт одному расширению код другого | **Л** | v0.5 | **✓** REQ-24, `packages/core-extensions/src/analyse.ts`, e2e/scn-017 (SCN-018) |
| 3.6 | `eval` / `new Function` / динамический код | статический разбор пакета, который приносит человек | **Л** | v0.5 | **✓** REQ-24, `packages/core-extensions/src/analyse.ts`, e2e/scn-017 (SCN-018) |
| 3.7 | Загрузка удалённого кода и C2 | внешние эндпоинты в коде (включая `ws:`/`wss:`) и загрузчики — `importScripts`, построение `<script>`, `chrome.scripting.executeScript` с файлами. **Сверки эндпоинтов с фидами нет** — B-56 | Л/П | v0.5 | **частично** REQ-24, `packages/core-extensions/src/analyse.ts` |
| 3.8 | Опасные комбинации прав | именованные пары над текущей описью (`cookies`+`webRequest`, `cookies`+`scripting`, `debugger`+`cookies`, …), серьёзность выше, если держатся на всех сайтах | **Л** | v0.5 | **✓** `packages/core-extensions/src/standing.ts`, показывается строкой расширения |
| 3.9 | Доступ к AI-панелям и агентским поверхностям | флаг: расширение лезет к AI-сайдбару (эскалация привилегий через Gemini-панель — CVE-2026-0628) | **Л** | v0.5 | — |
| 3.10 | Sideload и установка вне стора | `installType` из `management`: `sideload` — critical, `development` — major, `admin` намеренно молчит (человек не может его убрать, и алярм без действия — шум) | **Л** | v0.5 | **✓** `packages/core-extensions/src/standing.ts`, показывается строкой расширения |
| 3.11 | Репутация: пользователи, рейтинг, возраст | метаданные стора | С | v0.5 | — |
| 3.12 | Предупреждение **до** установки | перехват страницы Web Store, показ риск-карточки | Л/С | v0.5 | — |
| 3.13 | Действие | выключить (`setEnabled`), закрепить версию в наблюдении, добавить в доверенные | **Л** | v0.5 | — |

## 4. Учётные данные и утечки

| # | Вектор | Что делаем | Метод | Реж. | Веха | Готово |
|---|---|---|---|---|---|---|
| 4.1 | Утёкший пароль | предупреждение + кнопка смены | локальный корпус Pwned Passwords (**ноль запросов**) либо k-anon (5-символьный префикс SHA-1 + `Add-Padding`) | **Л**/П | v0.4 | **✓** REQ-20, `packages/core-credential/src/password.ts`, `packages/core-credential/src/sha1.ts` — **кроме формы во встроенном фрейме**: проверка висит на `submit` в верхнем фрейме, B-80 |
| 4.2 | Переиспользование пароля | «этот же пароль на N сайтах» | локальные солёные хэши, наружу ничего | **Л** | v0.4 | **✓** REQ-21, e2e/scn-015 |
| 4.3 | Email/телефон в утечках | список брешей с тегами данных | HIBP + XposedOrNot | П/С | v0.4 | **половина** — REQ-21: HIBP и Hudson Rock есть, SCN-015; XposedOrNot и телефоны — нет, поэтому не отмечено |
| 4.4 | **Infostealer-логи** (свежие заражения, сессии, куки) | отдельный красный алерт | Hudson Rock Cavalier (бесплатно, без ключа) | С | v0.4 | **✓** REQ-18, e2e/scn-011 |
| 4.5 | Что делать после утечки | очередь действий: сменить пароль (`/.well-known/change-password`), проверить повтор, включить 2FA, отозвать сессии | плейбуки ассистента | **Л** | v0.4 | **✓** REQ-22, SCN-016, SCN-025, e2e/scn-025 |

## 5. AI-слой (надстройка, не замена базового)

| # | Вектор | Что делаем | Метод | Реж. | Веха | Готово |
|---|---|---|---|---|---|---|
| 5.1 | Indirect prompt injection на странице | детект + показ «где спрятано» | DOM-vs-render diff → правила/сигнатуры → ONNX-классификатор | **Л** | v0.1 | **✓** REQ-03/04, e2e/scn-003 |
| 5.2 | Инструкции в нерендеримом HTML (комментарии, `meta`, `alt`, `aria`, JSON-LD) | то же | разбор носителей | **Л** | v0.1 | **✓** REQ-10, e2e/scn-005 |
| 5.3 | Zero-width и Unicode-теги, разрыв токенов | то же | нормализация Unicode + классы символов | **Л** | v0.1 | **✓** REQ-11, e2e/scn-010 |
| 5.4 | Чтение страницы агентом/сайдбаром | санитайзер: вычистить скрытые инструкции до чтения | правка DOM в режиме агента | **Л** | v0.2 | **✓** REQ-10, SCN-005, `apps/extension/src/content/sanitize.ts`, `packages/core-sanitizer/src/plan.ts` |
| 5.5 | Чувствительное действие агента на заражённой странице | стоп-кран, явное подтверждение | связка «найдена инъекция» + событие действия | **Л** | v0.2 | **✓** REQ-11, SCN-010, `apps/extension/src/content/agent-gate.ts`, `packages/core-gate/src/decide.ts`; предел записан в ADR-0009 |

## Сверка 2026-08-20 (B-56)

**24 отметки из 42 векторов**, и число считается командой ниже, а не помнится.
Изменение этого захода — блок Extension Guard: было 3 из 13, стало 7 из 13, из
них две построены сегодня, а три отметки просто не стояли при работающем коде.

## Сверка 2026-08-13

**Столбец «Готово» не поддерживался с 2026-08-03 и занижал.** Пять строк
проверены против кода в этот заход: 1.15, 4.3, 4.5, 5.4 и 5.5 были построены и
стояли без отметки. Читать матрицу как состояние продукта нельзя было ни в одну
из сторон — она завышала намерением и занижала отметкой.

Остальные двадцать строк перепроверены и **остаются без отметки**: кода под ними
нет. Это не список ближайших задач — веха в столбце «Веха» (`v0.3`, `v0.5`,
`v0.7`) осталась планировочной и по-прежнему ничему в репозитории не
соответствует.

**Extension Guard пересчитан 2026-08-20 (B-56): 7 из 13, а не 3.** Матрица
занижала: обфускация, `eval` и удалённый код проверяются `analysePackage` и
достижимы из интерфейса — не разбором установленного CRX, которого нет и быть не
может (браузер не отдаёт одному расширению код другого), а разбором пакета,
который человек приносит сам. Строки описывали механизм, которого не существует,
и потому стояли без отметки при работающей проверке.

В том же заходе построены **3.8** (опасные комбинации прав) и **3.10** (sideload):
оба считаются из данных, которые браузер уже отдаёт, без разбора пакета и без
сети. Оба — факты **о состоянии**, а не об изменении, и до этого дня продукт
сообщал только дельты: расширение, поставленное сторонней программой, не
«менялось» ни разу и потому не упоминалось ни разу.

**Четыре записи остаются без кода, и по каждой записано решение, а не молчание:**
3.4 (тихое обновление после затишья) — нужна история дат релизов, которой у
`management` нет; 3.9 (доступ к AI-панелям) — нужен перечень таких поверхностей,
и он устареет быстрее релизного цикла; 3.11 (репутация стора) — сеть на каждое
расширение и профиль пользователя у чужого сервиса, что противоречит
[ADR-0002](adr/0002-the-worker-never-signs.md) и обещанию приватности; 3.12
(предупреждение до установки) — контент-скрипт на страницах магазина, то есть
разрешение, которого продукт не просит. Каждое — **отказ с причиной**, а не
задача, и веха `v0.5` у них снята.

Способ пересчитать, а не вспомнить:

```
grep -cE '^\| [0-9]+\.[0-9]+ ' docs/coverage-matrix.md          # всего векторов
grep -E '^\| [0-9]+\.[0-9]+ ' docs/coverage-matrix.md | grep -c '✓'
```

## 6. Чего осознанно НЕ делаем

- Антивирус файловой системы, лечение уже заражённого устройства.
- Кредитный мониторинг, страховка от кражи личности (привязаны к США).
- Блокировка рекламы как продукт (это делают бесплатные, у нас — только
  вредоносные редиректы).
- Анализ содержимого писем через облачный OAuth (противоречит принципу P1).
- Хостинг и перепродажа чужих дампов.

## 7. Как это читать вместе с дорожной картой

Базовый набор (разделы 1–4) — **обязательный минимум продукта**, без него
AI-слой не имеет смысла: пользователя всё ещё разведут обычным фишингом.
AI-слой (раздел 5) — то, чем мы отличаемся и почему идём первыми.

Порядок вех в [product-vision.md](product-vision.md) §6 отражает не важность, а
последовательность сборки: сначала уникальное ядро (v0.1–v0.2), затем быстро
добираем полный базовый набор (v0.3–v0.5), дальше усиления (v0.6–v0.7).
