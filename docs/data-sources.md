# Реестр источников данных

Рабочий реестр для реализации. Цены — ориентир на 2026-08, перед закупкой
перепроверять. Статусы: `выбран` / `кандидат` / `отклонён` / `не решено`.

Легенда приватности: ★★★ — наружу не уходит ничего идентифицирующего;
★★ — уходит анонимизированный примитив (префикс хэша); ★ — уходит plaintext
идентификатор (email/URL).

---

## 1. Утечки и скомпрометированные креденшелы

### 1.1 Бесплатные

| Источник | Что даёт | Доступ | Лимиты / цена | Приватность | Статус |
|---|---|---|---|---|---|
| **HIBP Pwned Passwords** | ~1B+ хэшей паролей (SHA-1 и NTLM) с частотой | `api.pwnedpasswords.com/range/{5 hex}` **или полный корпус** через `PwnedPasswordsDownloader` | без ключа, **без рейт-лимита**, атрибуция не требуется | ★★★ офлайн / ★★ через API (k-anon + `Add-Padding: true`) | кандидат (базовый) |
| **HIBP breach metadata** | 1022+ брешей: описания, даты, `dataClasses` | `/breaches`, `/breach/{name}`, `/latestBreach`, `/dataClasses` | бесплатно, без ключа | ★★★ (публичные факты) | кандидат |
| **Hudson Rock Cavalier Community** | **infostealer-логи** по email / домену / username | `cavalier.hudsonrock.com/api/json/v2/osint-tools/search-by-{email,domain,username}` | **без ключа**, 50 req / 10 сек | ★ (уходит plaintext email) — нужен прокси/OHTTP | кандидат (высокая ценность) |
| **Hudson Rock free monitoring** | алерты по 3 email + 3 домена + 3 username | веб-кабинет | бесплатно | ★ | кандидат |
| **XposedOrNot** | breach-поиск по email, экспозиция паролей, domain-риск; **open-source**, SDK Go/Rust/.NET | JSON API | бесплатный тариф; платный `xonAPI+` | ★★ (есть anon-режимы) | кандидат |

### 1.2 Платные

| Источник | Покрытие | Цена (ориентир) | Замечания | Статус |
|---|---|---|---|---|
| **HIBP API** (Core / Pro / High-RPM) | email→breach, k-anon email search, **stealer logs**, domain search | от ~$4.4/мес; enterprise-поиск ~$0.05 | stealer logs и k-anon email — только Pro+; данные под **CC BY 4.0 — атрибуция обязательна** | кандидат |
| **DeHashed** | email, username, IP, телефон, пароли/хэши | ~$0.02–0.25 за запрос, pay-as-you-go | подходит как «глубокая проверка по требованию» | кандидат |
| **LeakCheck** | 7B+ записей: email / username / phone / keyword / domain | $9.99/мес (200 email/день); lifetime $24.99 | дешёвый вход | кандидат |
| **Snusbase** | комбо-дампы, поиск по многим полям | $5–16/мес, API до 2048 req/день | | кандидат |
| **Leak-Lookup / BreachDirectory** | агрегаторы дампов | дешёвые per-query | | кандидат |
| **Enzoic** | проверка креденшелов в момент логина/смены пароля | B2B-подписка | под наш кейс избыточно | отклонён |
| **Intelligence X** | архивы, darkweb, документы | от €2 500/год; API-тариф на порядок дороже | цена вне бюджета личного продукта | отклонён |
| **Breachsense / LeakRadar** | свежие stealer-логи, B2B-мониторинг | подписка | альтернатива Hudson Rock, если бесплатного не хватит | кандидат |

### 1.3 Ключевые выводы

1. **Infostealer-логи ≠ breach-дампы.** Свежие заражения (сессии, куки,
   автозаполнение) опаснее старых дампов. Guardio показывает их как
   «Available on the dark web» / «Source is hidden». Бесплатный источник —
   Hudson Rock Cavalier.
2. **Пароли проверяются полностью офлайн.** Корпус Pwned Passwords скачивается
   целиком → локальный индекс → ноль сетевых запросов. Это строго лучше, чем у
   Guardio.
3. **Стратегия — многоисточниковый агрегатор с дедупликацией:** HIBP
   (метаданные + email) + XposedOrNot (бесплатная база) + Hudson Rock (stealer)
   + один платный per-query как «глубокая проверка по требованию».

---

## 2. Приватные протоколы поиска

| Протокол | Как работает | Где применимо | Стоимость реализации |
|---|---|---|---|
| **k-anonymity** | SHA-1 → шлём префикс (5 hex для паролей, 6 для email HIBP) → получаем ~800 суффиксов → матчим локально; `Add-Padding` скрывает даже размер ответа | пароли (готово в HIBP), email (HIBP Pro+) | низкая |
| **OPRF / PSI** | клиент «ослепляет» хэш, сервер применяет PRF не узнавая ничего, клиент «разослепляет»; Google Password Checkup — кривая `secp224r1`, ~4B креденшелов; так же в Apple iCloud Keychain | нужен свой сервер | высокая |
| **Oblivious HTTP (OHTTP)** | релей видит IP но не контент, сервер — контент но не IP; Google даёт Safe Browsing OHTTP Gateway | любой внешний API, включая plaintext-эндпоинты Hudson Rock | средняя |
| **Локальный Bloom / Cuckoo-фильтр** | периодически тянем компактный фильтр всех «плохих» хэшей, проверяем офлайн, в сеть — только при попадании (и то через k-anon) | URL-репутация, breach-проверки | средняя |

**Рекомендация:** Bloom-фильтр + k-anonymity как база; OHTTP — для эндпоинтов,
где plaintext неизбежен; PSI — только если появится свой бэкенд.

---

## 3. URL-репутация, фишинг, вредоносные сайты

| Источник | Что даёт | Цена | Приватность | Статус |
|---|---|---|---|---|
| **Google Safe Browsing v5** | Local List mode (локальная БД хэш-префиксов), Real-Time, No-Storage RT; `hashes.search` до 1000 префиксов за запрос | **бесплатно, только non-commercial** | ★★★ в Local List / ★★ + OHTTP Gateway | кандидат (зависит от развилки Q4) |
| **Google Web Risk API** | коммерческий эквивалент GSB | 100k lookup/мес бесплатно, далее $0.50 / 1000 | ★ (Lookup шлёт URL) | резерв на случай коммерции |
| **OpenPhish community** | активные фишинговые URL | бесплатно, обновление ~12 ч | ★★★ (файл скачивается целиком) | кандидат |
| **PhishTank** | база фишинга + API | бесплатно; регистрация новых аккаунтов закрыта | ★★★ | кандидат |
| **URLhaus (abuse.ch)** | malware-URL | бесплатно | ★★★ | кандидат |
| **Phishing Army / phishing-filter / threat-hostlist** | агрегаты PhishTank + OpenPhish + CERT.pl + urlscan + Phishunt, обновление 6 ч | бесплатно | ★★★ | кандидат (основной пакет для DNR) |
| **urlscan.io** | полный скан страницы, вердикт, скриншот | free tier + платные | ★ (публичные сканы по умолчанию!) | только для ручного разбора |
| **Cloudflare Radar URL Scanner** | скан по API | в составе Cloudflare | ★ | резерв |

### 3.1 Проактивный слой (нет у Guardio)

| Источник | Что даёт | Цена |
|---|---|---|
| **CertStream (Calidog)** | real-time поток Certificate Transparency — каждый новый TLS-сертификат в момент выпуска; фишинг получает сертификат за часы до атаки | бесплатно |
| **whoisds NRD** | дневной список новых доменов (только gTLD, без WHOIS/DNS) | бесплатно |
| **whoisfreaks NRD** | то же + WHOIS/DNS | платно |
| **dnstwist** | генерация typosquat-вариантов вокруг конкретных брендов | open-source |
| **Tranco** | список популярных доменов: аллоулист + база для «похож на топ-бренд, но не он» | бесплатно |

Возраст домена < 30 дней — сильнейший единичный признак фишинга.

---

## 4. Расширения браузера

| Источник | Что даёт | Цена | Статус |
|---|---|---|---|
| `chrome.management` | локальный инвентарь + `setEnabled()` | — | выбран (безальтернативно) |
| **chrome-stats.com** | метаданные стора: users, rating, история версий, **смена владельца** | free/платно | кандидат |
| **CRXPlorer** | бесплатный сканер CRX: разрешения, подозрительный код | бесплатно | кандидат |
| **Secure Annex / ExtensionTotal / Spin.AI / LayerX** | коммерческий risk-scoring | подписка | резерв |
| **CRXcavator (Duo)** | — | **мёртв с 2023** | отклонён |
| **Свой статический анализ** | скачать CRX → распаковать → `permissions`, `host_permissions`, `eval`, обфускация, удалённые эндпоинты, **дельта разрешений между версиями** | своя разработка | кандидат (ключевое преимущество) |

Главный сигнал заражения в 2026: **смена владельца расширения + резкий рост
разрешений в новой версии**.

---

## 5. Почта и SMS

| Источник | Что даёт | Приватность | Статус |
|---|---|---|---|
| Gmail API (OAuth read-only) | как у Guardio | ★ — самое чувствительное разрешение продукта | не решено (Q6) |
| IMAP (Proton Bridge, Fastmail, свой) | разбор локально | ★★★ | не решено (Q6) |
| SPF / DKIM / DMARC | валидация отправителя | ★★★ локально | кандидат |
| MalwareBazaar | проверка вложений по хэшу | ★★ (хэш) | кандидат |

Ссылки из писем проверяются теми же URL-фидами (§3).

---

## 6. Правовые ограничения

- Хранение сырых дампов «email:пароль» — обработка чужих персональных данных в
  больших объёмах; под GDPR хранение сверх необходимого прямо является
  основанием для санкций.
- **Безопасный дизайн:** не хостим чужие дампы; храним только хэши/префиксы и
  метаданные брешей; свои идентификаторы проверяем через API источников;
  результаты пользователя шифруем локально.
- **HIBP breach/paste API — CC BY 4.0**: обязательна видимая атрибуция со
  ссылкой на haveibeenpwned.com везде, где показываются эти данные.
  Pwned Passwords — атрибуция не требуется.
- **Safe Browsing v5 — только non-commercial.** Коммерческое использование →
  Web Risk. Также GSB требует показывать предупреждение с определённой
  формулировкой и атрибуцией Google.
- Hudson Rock Community API — свободен для community-использования; при
  коммерциализации условия перепроверить.

---

## 7. AI-эпоха: детект скрытых инструкций (prompt injection)

Ядро продукта (см. [product-vision.md](product-vision.md) §4.1). Готового
коммерческого фида «сигнатур инъекций» на рынке нет — это одновременно риск и
защитный ров: свой корпус и свои сигнатуры становятся активом.

### 7.1 Исследовательская база (данные для правил и корпуса)

| Источник | Что даёт | Как используем |
|---|---|---|
| «Indirect Prompt Injection in the Wild» (arXiv 2604.27202) | 1.2 млрд URL / 24.8 млн хостов → 15.3k подтверждённых внедрений на 11.7k страниц; ~70% в нерендеримом HTML; повторяющиеся шаблоны; цели атак | таксономия носителей, стартовый набор сигнатур, оценка базового уровня FP |
| Замер видимости (обзорные работы 2026) | лишь ~5.1% внедрений видимы пользователю, 58.6% скрыты приёмами рендеринга | обоснование ступени DOM-vs-render diff как основной |
| PhantomLint (arXiv 2508.17884) | принципиальный детект скрытых промптов в структурированных документах | методика для детерминированной ступени |
| IPI-proxy (arXiv 2605.11868) | перехватывающий прокси для ред-тиминга браузерных агентов | основа для нашего тестового стенда и регрессий |
| OWASP GenAI (LLM01:2025) + OWASP Top 10 for Agents 2026 | классификация, рекомендованные митигации (least privilege, human approval, семантический фаервол) | язык описания угроз в UI и структура плейбуков ассистента |
| Unit 42, Brave/Comet PoC, OpenAI Lockdown Mode (13.02.2026) | реальные инциденты и признание неустранимости изнутри | материал для онбординга и позиционирования |

### 7.2 Модели-классификаторы

| Модель | Размер | Пригодность в браузере | Замечания |
|---|---|---|---|
| **Llama-Prompt-Guard-2-22M** | 22M (DeBERTa-xsmall) | **основной кандидат** — ONNX + WebGPU/WASM | −75% латентности к 86M при малой потере качества; устойчивость к атакам на токенизацию (пробелы, фрагментация) |
| Llama-Prompt-Guard-2-86M | 86M | запасной для десктопа | выше качество, выше цена |
| ProtectAI `deberta-v3-base-prompt-injection-v2` | 184M | **отклонён для базовой поставки** | ~1 ГБ + GPU |
| Rebuff | — | **отклонён** | проект архивирован в мае 2025 |

Известное ограничение: guardrail-модели обходятся адверсариальными приёмами
(есть отдельная работа по evasion). Поэтому классификатор — **третья** ступень,
а не единственная; детерминированный diff нельзя обойти, не показав текст
человеку.

### 7.3 Локальный рантайм для ассистента

| Опция | Требования | Статус |
|---|---|---|
| Детерминированные плейбуки | нет | **база, работает всегда** |
| ONNX Runtime Web (WASM/WebGPU) + 22M-классификатор | GPU желателен | основной путь |
| **Chrome Built-in AI (Gemini Nano)**: `LanguageModel`, `Summarizer`, `Writer`, `Rewriter`, `Proofreader` | Chrome 138+, Win10/11 / macOS 13+ / Linux; **≥22 ГБ свободного диска, ≥4 ГБ VRAM**; в расширениях — через origin trial; **не** Android/iOS/ChromeOS | усиление, не зависимость; полностью на устройстве |
| Свой API-ключ пользователя (облако) | явный опт-ин | только по прямому включению |

### 7.4 Проверка файлов и загрузок

| Источник | Что даёт | Лимиты / цена | Замечания |
|---|---|---|---|
| **MalwareBazaar (abuse.ch)** | поиск по хэшу файла | бесплатно, нужен Auth-Key с `auth.abuse.ch`; fair use; загрузка файлов — 2000/сутки на IP | коммерческое использование — платный abuse.ch API |
| **VirusTotal Public API v3** | вердикты по **хэшу** | 500 запросов/сутки, 4/мин, **non-commercial**, нельзя использовать в бизнес-процессах, не пополняющих корпус | шлём только хэш, файл — никогда |
| **URLhaus** | вредоносные URL раздачи | бесплатно | проверка `finalUrl` загрузки |
| `chrome.downloads` + `onDeterminingFilename` | `finalUrl`, MIME, имя файла, момент перед сохранением | — | расширение **не читает файл с диска**: хэш считаем по повторно скачанным байтам (работает для прямых ссылок), иначе — URL-репутация и эвристики (двойное расширение, несоответствие MIME) |
| Safe Browsing Download Protection | — | **недоступно расширениям** | как источник не рассматривается |
