# Pipeline brief — Okolos P0–P5 + public domain status

- **Дата:** 2026-08-04
- **Стадия:** 0 (intake) — бриф на замок
- **Модель прогона:** Opus 5 (подтверждено на preflight)

## 1. Задача

MV3-расширение (Chrome/Edge/Firefox) + тонкий Cloudflare Worker. Безопасность
браузинга в эпоху AI-агентов: детект и очистка скрытых инструкций для
AI-агентов, базовый анти-скам (ссылки, страницы, загрузки), утёкшие пароли и
данные, контроль расширений, дружелюбный ассистент, публичная страница статуса
домена с апелляциями. Опенсорс AGPL-3.0, английский UI, локальность
проверяема.

## 2. Source ledger (харвест стадии 0)

| Источник | Состояние | Вклад |
|---|---|---|
| git | не инициализирован | ни истории, ни веток |
| `graphify-out/graph.json` | отсутствует | — (решено строить с первого модуля) |
| `CLAUDE.md` проекта | есть | super-ux hard rule: сценарии — источник правды, линтер обязателен |
| `CLAUDE.md` глобальный | есть | роутинг через task-pipeline, production-grade планка, один wiki-vault |
| `CONTEXT.md`, ADR | отсутствуют | — |
| `docs/` (16 файлов, 3511 строк) | написаны 2026-08-02…04 | вижен, матрица покрытия, реестр источников, конкуренты, evidence [A]/[B]/[C] |
| `docs/ux/` | линтер зелёный | 3 персоны · 8 JTBD · 3 журнея · 20 сторей · 17 флоу · 14 экранов · 26 сценариев |
| `docs/superpowers/` | отсутствовал | **standing instructions отсутствуют** — прогон ничем не связан |
| obsidian wiki | установлен, vault найден | записи об okolos нет → стадия 9 создаёт `projects/okolos` |

Стейл-документов не найдено.

## 3. Решения intake-грилла

| # | Решение | Значение |
|---|---|---|
| D-1 | Репозиторий | публичный GitHub **`ssheleg/okolos`** (аккаунт `sshlg`, членство в орге подтверждено, имя свободно) |
| D-2 | Деплой воркера | **API-токен в env** (`CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`), аккаунт Sergeysheleg4@gmail.com. Human step 1 закрыт 2026-08-07; деплой автоматизирован — `node tools/deploy-worker.mjs`, раннбук `docs/runbooks/worker-deploy.md` |
| D-3 | Трекер | **GitHub Issues** |
| D-4 | Дистрибуция | **GitHub Releases** сейчас; Chrome Web Store и AMO — с R3 |
| D-5 | Граф кода | **graphify с первого модуля**, обновляется на стадии 9 |
| D-6 | Дизайн-инструмент | текст + ASCII-вайрфреймы; Figma включается перед написанием UI-кода |
| D-7 | Архитектура | монорепо, портируемое ядро без браузерных API + адаптеры Chrome/Firefox + воркер (согласовано в брейншторме) |
| D-8 | Лицензия | AGPL-3.0 (включая код воркера) |

**Решения, принятые по умолчанию (конвенциональные, без вопроса):**
TypeScript strict, pnpm workspaces, vitest + fast-check, Playwright для e2e
расширения, ESLint с правилом границы `core-* ↛ browser API`, Prettier;
trunk-based, `main` защищён, работа в ветках `feat/*`, PR с гейтами CI,
Conventional Commits, теги `v0.x`; логи — GitHub Actions + локально
`.pipeline/logs/`.

## 4. REQ table

Каждая строка адресуема и названа вместе со способом проверки. Список
заморожен: добавлять свободно, убирать — по согласию оператора.

| REQ | Требование | Как проверяется | Релиз |
|---|---|---|---|
| REQ-01 | Монорепо с границей: `core-*` не импортирует браузерные API | lint-правило + CI-джоб падает на нарушении | R1  — **DONE** |
| REQ-02 | Контракты `Verdict`/`Evidence`/`PageCandidates`/RPC v1 + схема хранилища v1 | unit на сериализацию; миграция применяется на чистом профиле | R1  — **DONE** |
| REQ-03 | Ступень 1: DOM-vs-render diff | корпус инъекций: recall ≥90%; FP <1% на топ-1000 | R1  — **DONE** |
| REQ-04 | Ступень 2: правила и сигнатуры (zero-width, Unicode-теги, императивы, шаблоны кампаний) | корпус + unit | R1 — **DONE**; 2026-08-08 добавлен русский во все девять сигналов: они были написаны только по-английски, и пять русских форм атаки давали ноль сигналов — в детекторе, которым продукт назван, и для аудитории, для которой написан его интерфейс. Ограничение по языкам названо в SCN-003 |
| REQ-05 | Ступень 3: ONNX-классификатор 22M, запуск только по сигналу ступеней 1–2 | тест «без сигнала инференса нет»; бенч ≤250 мс | R1  — **DONE** |
| REQ-06 | Баннер SCR-03 (injection) и инспектор SCR-04 | e2e SCN-003, SCN-004 | R1  — **DONE** |
| REQ-07 | Первый запуск SCR-01: результат <30 с без аккаунта | e2e SCN-001, SCN-002 + бенч | R1  — **DONE** |
| REQ-08 | Единая точка выхода в сеть + журнал самоаудита SCR-10 | e2e с перехватом сети: запрос без записи в `outbound_log` роняет билд (SCN-019) | R1  — **DONE** |
| REQ-09 | Бюджеты производительности (diff ≤8/20 мс, правила ≤5 мс, память SW ≤64 МБ) | бенч в CI, регресс = красный | R1  — **DONE** |
| REQ-10 | Санитайзер с полной обратимостью | e2e SCN-005 + render-diff по топ-1000 (проверка допущения A-4) | R2  — **DONE** |
| REQ-11 | Гейт действия агента; таймаут = Block | e2e SCN-010 | R2  — **DONE** |
| REQ-12 | Попап SCR-02, журнал и дифф SCR-11, очередь ≤3 (SCR-07) | e2e SCN-020, 021, 022 | R2  — **DONE** |
| REQ-13 | Фиды: подпись Ed25519, дельты, откат на прошлый проверенный при невалидной подписи | unit + негативный тест с подделанной подписью | R3 — **DONE**; ключ и подписывающий инструмент 2026-08-07. **2026-08-08 обнаружено, что проверять было нечего:** `updateFeed` не вызывался ниоткуда кроме собственных тестов, хранилище фидов оставалось пустым на каждой установке. Путь построен — `feed-sync.ts`, будильник каждые 6 часов и один раз на старте, отказ оставляет прежний снимок в силе и пишется в журнал |
| REQ-14 | Блок известного фишинга SCR-05 + запоминаемые исключения | e2e SCN-007 | R3 — **DONE**; **2026-08-08:** до этой даты строка была верна только в тесте, который сажал фид в хранилище руками. В продукте фид никто не загружал, `currentFeed()` всегда возвращал null, и число блокирующих правил всегда было нулём. Загрузка построена; e2e остаётся с посевом, потому что он проверяет блокировку, а не доставку |
| REQ-15 | Punycode, confusables, typosquat | корпус пар + e2e SCN-006 | R3 — **DONE** |
| REQ-16 | Перехват ClickFix (запись в буфер + шаблон) | корпус ловушек + e2e SCN-008 | R3 — **DONE**; 2026-08-08 добавлен русский: шаблон страницы читал только английский, а список наблюдения содержит `sberbank.ru` и `gosuslugi.ru` — то есть кампания на языке аудитории проходила чисто. Ограничение по языкам названо в SCN-008 |
| REQ-17 | Разрыв tech-support ловушек (fullscreen, циклические диалоги) | e2e SCN-009 | R3 — **DONE**; циклические диалоги фиксируются и называются, но не подавляются: они поднимаются в мире страницы |
| REQ-18 | Защита ввода пароля и карты на недоверенном домене | e2e SCN-011 | R3 — **DONE** |
| REQ-19 | Проверка загрузок (URL, тип/MIME, хэш) с честным частичным вердиктом | e2e SCN-012, SCN-013 | R3 — **DONE, покрытие unit**: реальная загрузка через расширение в Playwright нестабильна; хэш на этом этапе не считается никогда — файла ещё нет, и это отражено в вердикте |
| REQ-20 | Офлайн-проверка паролей + k-anon с `Add-Padding` | e2e SCN-014 + тест «при локальном попадании 0 сетевых запросов» | R4 — **DONE**, покрытие unit: тест «локальное попадание → 0 запросов и 0 записей в аудит» есть |
| REQ-21 | Мониторинг утечек мультиисточник с честным раскрытием покрытия | e2e SCN-015, SCN-016 | R4 — **DONE**; ключ HIBP = human step, без него источник честно сообщает причину |
| REQ-22 | Чеклист восстановления SCR-13 | e2e SCN-025 | R4 — **DONE** |
| REQ-23 | Инвентарь расширений, дельта разрешений, смена издателя | e2e SCN-017, SCN-018 | R5 — **DONE, покрытие unit**: другие расширения в e2e-профиле не установить |
| REQ-24 | Статический разбор CRX: обфускация, `eval`, удалённый код, эндпоинты | unit на фикстурах | R5 — **DONE**; браузер не отдаёт пакет чужого расширения — единственный runtime-путь у анализатора это файл, который выбрал пользователь («Inspect package» на SCR-09, читается на устройстве). До 2026-08-05 анализатор не имел вызова вообще: требование считалось закрытым по фикстурам, а код был недостижим |
| REQ-25 | Воркер: фиды, модели, прокси, `/status/domain`, `/appeal` (D1, retention 180 дней) | тесты воркера + деплой-смоук | R5 — **DONE** 2026-08-07: выкачен на `okolos-proxy.sergeysheleg4.workers.dev`, D1 `okolos` создана и схема накатана, cron `17 3 * * *` зарегистрирован; смоук из 4 проверок в `tools/deploy-worker.mjs`, каждая отличает этот воркер от чужого хоста (проверено натравливанием на `example.com`) |
| REQ-26 | Публичная страница статуса домена и апелляции без аккаунта | e2e SCN-026 | R5 — **DONE 2026-08-08**: до этой даты строка была закрыта на рендерере, который никто не вызывал — ни HTML, ни точки входа, ни запроса к воркеру. Страница теперь отдаётся самим воркером по `/status?domain=x` с ответом в разметке: её читает и человек, и краулер, без JavaScript. Канонический адрес на нормализованный домен |
| REQ-27 | Кросс-браузер: Chrome/Edge и Firefox из одного ядра | e2e-прогон обоих билдов: Chromium через Playwright, Firefox через geckodriver | R1→R5 — **DONE** — Firefox-набор зелёный (`pnpm test:e2e:firefox`), 9 проверок, включая гейт агента и санитайзер |
| REQ-28 | Приватность: ноль исходящих с URL или содержимым; экспорт и стирание данных | сетевой гейт + e2e SCN-019, SCN-023, SCN-024 | R1→R5  — **DONE** — сетевой гейт + SCN-019/023/024 |
| REQ-29 | Доступность WCAG 2.2 AA на всех поверхностях | axe-прогон в e2e | R1→R5 — **DONE** — axe по 5 страницам расширения плюс три оверлея (SCR-03/04/06) на странице с враждебным CSS, 2026-08-07; ярлык чекбокса чинился по факту падения |
| REQ-30 | AGPL-3.0 + атрибуция HIBP (CC BY 4.0) в UI и доках | наличие LICENSE + UI-тест на строку атрибуции | R1→R5  — **DONE** |
| REQ-31 | UX-цепочка не дрейфует | `python3 docs/ux/lint.py` в CI | R1→R5  — **DONE** |
| REQ-32 | Стирание данных достижимо из UI настроек с подтверждением, называющим удаляемое | e2e SCN-023 | R2  — **DONE** |
| REQ-33 | Потолок памяти фонового контекста (≤64 МБ) измеряется, а не предполагается | `e2e/memory.spec.ts`: 30 навигаций, затем `Runtime.getHeapUsage` воркера через DevTools-порт; измеряется JS-heap, не весь процесс | R1  — **DONE** |
| REQ-34 | Firefox e2e — чтобы REQ-27 опирался на тест, а не на сборку | `tools/firefox-e2e.mjs` через geckodriver: 9 проверок — установка расширения, SCN-003, недосягаемость баннера со стороны страницы, санитайзер (текст ушёл из DOM + элемент помечен), гейт агента (скриптовый submit удержан + страница не ушла), тишина на чистой странице (баннер и гейт) | R1 — **DONE**; расширено 2026-08-08 до путей, где движки расходятся: фоновая страница вместо service worker, доставка скриптового клика, планирование MutationObserver. Каждая из четырёх новых проверок подтверждена подсаженным дефектом |
| REQ-35 | Тестовый хук, открывающий shadow root в e2e-сборке, чтобы путь «баннер → инспектор» проходился кликом, а не подразумевался | e2e SCN-004 полностью, без оговорок | R1 — **DONE** |
| REQ-36 | Доставка весов классификатора: согласие пользователя, аудируемая загрузка, закреплённый дайджест, кэш с версией | `packages/model` — 10 тестов, включая отказ без согласия и отбраковку по дайджесту | R1  — **DONE** |
| REQ-37 | ONNX-сессия поверх доставленных весов: offscreen в Chrome, фоновая страница в Firefox, WebGPU→WASM фолбэк | бенч ≤250 мс + прогон по корпусу с измеренным качеством | R2 — **PARTIAL**: сессия, фолбэк, выбор хоста, кэш весов и честная недоступность собраны и покрыты тестами. **Лицензионный вопрос закрыт 2026-08-08** — политика в `docs/licences.md`, загейчена; остаётся инженерная часть: единственный ungated Apache-2.0 артефакт — 738 563 308 байт, малые варианты gated (401 без авторизации), поэтому нужна собственная INT8-квантизация, которую Apache-2.0 разрешает при сохранении атрибуции |

**Развилка закрыта 2026-08-08 (human step 4).** Политика: проект несёт только
веса, которые можно перераспространять и производные от которых разрешены, без
принятия пользователем чужих условий — публичный AGPL-репозиторий не может
согласиться на acceptable-use за своего пользователя. Семейство Llama, включая
`Llama-Prompt-Guard-2-22M`, исключено этим правилом.

Измерения показали, что лицензия и размер здесь — **один вопрос**: единственный
ungated Apache-2.0 артефакт весит 738 563 308 байт, а малые варианты того же
семейства отдают 401 без авторизации. Apache-2.0 разрешает производные при
сохранении атрибуции, поэтому открытый путь — собственная INT8-квантизация:
сначала измерить размер и качество, потом решать. Если не сойдётся с бюджетом
REQ-09, третья ступень не выпускается, и честная деградация для этого уже
построена и покрыта тестами.

Решение, доказательства и обязательства Apache-2.0 §4 — в
[../../licences.md](../../licences.md). Дескриптор модели указывает на
`pending-quantisation.onnx` с нулевым дайджестом: артефакта ещё нет, и врать
про него нечего.

REQ-36 выделен из REQ-05 приёмкой: гейт запуска, деградация и потолок стоимости
реализованы и покрыты тестами, сам артефакт модели — нет. Смешивать «условие
запуска работает» и «модель есть» в одной строке значило бы отчитаться о
половине как о целом.

Строки REQ-32…34 добавлены приёмкой 2026-08-04: ladder walk нашёл тонкие
ступени и превратил их в требования, а не в сноски. Отчёт —
[../audits/2026-08-04-acceptance.md](../audits/2026-08-04-acceptance.md).


### Найдено обходом «обещано против реализовано» (2026-08-05)

Карточки экранов называют контролы, которых рендереры не рисуют. Закрыто в этот
прогон: SCR-07 «resolve» и «not now», SCR-09 «Inspect package». Остаётся:

| Экран | Обещано в карточке | Состояние |
|---|---|---|
| SCR-08 | группировка «свежее — инфостилер» / «исторические утечки» | **закрыто 2026-08-06** — `core-leaks/group`, e2e/scn-015 |
| SCR-08 | «Change password», «Check reuse» | **закрыто 2026-08-06** — `/.well-known/change-password`, когда источник назвал домен; «Mark resolved» переименован под карточку |
| SCR-12 | список доверенных доменов с «this is legitimate» | **закрыто 2026-08-06** — `ui/trusted`, RPC `trust/revoke`, e2e/scn-024 |

Это не оговорка к готовому — это незакрытый бэклог, найденный после приёмки.

## 5. Carry-over ledger (отложено осознанно)

| Пункт | Причина | Когда |
|---|---|---|
| Web3-слой: дрейнеры, опасные `approve` | ниша не SEO-канал, не ядро | после R5 |
| Проактив: CertStream, NRD, персональный typosquat-watchlist | требует фонового конвейера | R6 |
| Синк устройств и режим опекуна (ST-020) | `could`, есть kill criteria | R7 |
| OCR для картинок и скриншотов | тяжело, отдельная веха | позже |
| Почта (IMAP) | противоречит P1 в простом исполнении | позже |
| Парольная фраза для журнала | пока нет сценария, который она закрывает | позже |
| Публикация в Chrome Web Store и AMO | решено D-4 | с R3 |
| Figma-макеты всех экранов | решено D-6 | перед UI-кодом |
| ~~e2e под Firefox~~ | **закрыт 2026-08-04** через geckodriver: `pnpm test:e2e:firefox` | — |

## 6. Human steps (изолированы, автономный путь без них полон)

1. **Cloudflare API-токен** — создать (Workers Scripts:Edit, KV:Edit, D1:Edit),
   положить `CLOUDFLARE_API_TOKEN` и `CLOUDFLARE_ACCOUNT_ID` в env и в GitHub
   Secrets. Нужен к **R5**.
2. **Ключ подписи фидов Ed25519** — приватный ключ в GitHub Secrets, публичный
   вшивается в сборку. Нужен к **R3**.
3. **Chrome Web Store developer account** ($5 разово) — нужен к публикации в
   стор, **R3**.

## 7. Гейты

- Стадия 0 → 1: **manual** (замок брифа оператором)
- Стадии 1–7: **auto** (проверяю сам по критериям стадии)
- Стадия 8 (деплой): **manual** — трогает внешнее
- Стадии 9–10: **auto**, финальная приёмка с walk по REQ-таблице
