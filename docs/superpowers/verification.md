# Леджер верификации

Что отгружено — и кто, кроме прогона тестов, это подтвердил. Один ряд на
поведение, которое обещано пользователю.

**Зачем отдельно от тестов.** Зелёный тест говорит, что код делает то, что
написано в тесте. Он не говорит, что человек это видел. Столбец
«подтверждено» — про второе: `never` значит, что поведение живёт только в
утверждении, которое написал тот же, кто писал код.

Заведён 2026-08-13, на прогоне дашборда: пайплайн потребовал леджер, а его в
проекте не было ни одного дня. Ряды ниже заполнены назад по тому, что реально
проверялось, а не по тому, что хотелось бы.

| # | Поведение | Чем держится | Подтверждено | Когда |
|---|---|---|---|---|
| V-01 | Адрес открывает свою область; неизвестный называет себя | `views.ts:routeFor`, `tools/options-routes.test.ts`, `e2e/scn-027.spec.ts` | **прогоном в браузере** — 8 адресов пройдены по одному | 2026-08-13 |
| V-02 | Обзор перечисляет восемь областей, у каждой состояние | `dashboard/overview.ts`, `e2e/scn-027.spec.ts` | **прогоном в браузере** | 2026-08-13 |
| V-03 | Непрочитанное состояние не выглядит как «пусто» | `dashboard/overview.ts`, `e2e/scn-030.spec.ts` | **прогоном в браузере** — подсажена нечитаемая запись восстановления; строка помечена `data-unread` и не повторяет слов тихой области | 2026-08-13 |
| V-04 | Фокус возвращается любому элементу после перерисовки | `keep-focus.ts`, `keep-focus.test.ts`, `e2e/scn-015.spec.ts` | **частично** — поле адреса проверено в браузере; кнопки очереди только юнит-тестом. Остаётся в B-20 | 2026-08-13 |
| V-05 | Нажатие видно до того, как придёт результат | `pending.ts`, `e2e/scn-030.spec.ts` | **прогоном в браузере** — атрибуты прочитаны изнутри той же задачи, что и нажатие; действие при этом дошло | 2026-08-13 |
| V-06 | Полоса внимания не превышает трёх и считает остаток | `overview.test.ts`, `e2e/scn-030.spec.ts` | **прогоном в браузере** — пять находок дают три строки и «…ещё 2»; худшая первой | 2026-08-13 |
| V-07 | Девять экранов расширения проходят axe | `e2e/a11y.spec.ts` | **прогоном** — 8 областей + попап, интерстишл, первый запуск | 2026-08-13 |
| V-08 | Ворота действительно гоняются в CI | `.github/workflows/ci.yml` | **прогоном** — впервые за 100 запусков; `e2e (chromium)` и `e2e (firefox)` зелёные | 2026-08-13 |
| V-09 | Подпись фида и отгруженный ключ — одна пара | `tools/feed-key.test.ts` | `never` **на CI** — секрет `OKOLOS_FEED_KEY` не заведён; локально проходит | — |
| V-11 | Фид в проде содержит настоящие домены, а не фикстуру | `tools/ingest.mjs`, `tools/publish-feed.mjs` | **прогоном на проде** — v5, 248 хостов; `/status` называет `cardiffsegurogrupoamigo360.vercel.app` числящимся | 2026-08-13 |
| V-12 | Хосты, которые нельзя блокировать целиком, в фид не попадают | `tools/ingest.test.ts` | **прогоном на проде** — `g5.lu`, `dropbox.com`, `github.com` отвечают `not-listed` | 2026-08-13 |
| V-10 | Публичные страницы живы | `tools/deploy-worker.mjs --smoke-only` | **прогоном** — 4 из 4; но смоук ходит в `/healthz`, `/status/domain` дважды и `/nope`, а не в перечисленные здесь `/`, `/privacy`, `/status`: расхождение записано в B-60, страницы отдельно опрошены живым GET 2026-08-19 (все 200) | 2026-08-12 |
| V-13 | Хук, охраняющий пуш, пропускает пуш | `.githooks/pre-push`, `tools/tree.mjs` | **прогоном** — четыре гейта хука прошли и push дошёл до `origin/main` (`4d77846`); до правки тот же хук отказывал на `tools/docs.test.ts` | 2026-08-19 |

**Один ряд из тринадцати остаётся `never`, и он останется им навсегда:** V-09 —
пара ключей подписи на CI. Это не долг, а следствие ADR-0002, и B-19 закрыта
решением не заводить секрет; проверяет пару `.githooks/pre-push`, то есть
единственная сторона, которая может. V-04 стоит «частично»: поле адреса
проверено в браузере, кнопки очереди — только юнит-тестом.

Считать, а не помнить — ряды и статусы берутся из таблицы выше:

```bash
grep -cE '^\| V-[0-9]+ ' docs/superpowers/verification.md
grep -E '^\| V-[0-9]+ ' docs/superpowers/verification.md | grep -c '`never`'
```

Прежняя формулировка говорила «два ряда из десяти» при двенадцати рядах и одном
`never` — число было переписано из прошлой версии, а не измерено, в документе,
чья тема — не принимать утверждение за наблюдение. Остаток B-60: у ряда с
истекающим сроком (V-11 — свежесть фида) нет столбца «годно до», а V-10
утверждает смоук по четырём страницам, которых `deploy-worker.mjs:130-162` не
проверяет.

**Что дала попытка наблюдать три состояния (2026-08-13, B-20).** V-03, V-05 и
V-06 закрыты прогоном — и по дороге нашёлся дефект, которого юнит-тесты видеть
не могли: **одна нечитаемая запись восстановления гасила всю полосу внимания**.
`openIncidents` бросал изнутри её собственного `try`, и восемь областей
сообщались нечитаемыми из-за одной испорченной строки настроек. Теперь запись
пропускается и считается, полоса показывает всё, что прочиталось, а строка
восстановления честно говорит, что не видит всех инцидентов. Это ровно та
причина, по которой ряд со статусом `never` — не формальность.
